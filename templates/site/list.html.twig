{% extends 'base.html.twig' %}

{% block title %}Posts List{% endblock %}
{% block header %}<h3>Posts List ✅</h3>{% endblock %}
{% block body %}

    <div class="row  m-5">
        <hr>
            {% for post in posts %}
                <div class="col-sm-4"><a href="/post/{{ post.id }}">{{ post.title|raw }}</a><hr></div>
            {% endfor %}
        <hr>
    </div>
{% endblock %}
